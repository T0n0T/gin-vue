version: '3'
services:
  devcontainer:
    image: golang-dev:latest
    network_mode: host
    privileged: true
    volumes:
      - ..:/workspaces:cached
      - ../../gopath:/go:cached
      - ../../.vscode-server:~/.vscode-server:cached
    working_dir: /workspaces
    command: sleep infinity
